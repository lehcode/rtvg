<?php
/**
 * Чердак страницы
 *
 * @author  Antony Repin
 * @version $Id: header.phtml,v 1.11 2013-04-03 04:08:16 developer Exp $
 *
 */
?>

<?php 
$this->inlineScript()
	->appendScript( Rtvg_Compressor_JSMin::minify( 'VK.Widgets.Like("vk_like", {type: "button", verb: 1, height: 18});' ));
?>

<div class="container-fluid">

	<div class="navbar span4">
		<div class="navbar-inner">
			<?php echo $this->nav_html; ?>
		</div>
	</div>
	
	<?php 
	// Login form
	if (isset($this->user) && $this->isAllowed('default:auth')){
	    $wrapClass = 'span3';
		if ($this->user->role == 'guest') {
		    $form = new Xmltv_Form_Login( array(
				'action'=>$this->url( array(), 'default_user_login'),
				'wrap_class'=>$wrapClass,
			));
		} else {
			$form = new Xmltv_Form_Logout(array(
				'action'=>$this->url( array(), 'default_user_logout'),
				'wrap_class'=>$wrapClass,
			));
		}
		echo $form;
	}
	?>
	
	<div class="span2">
		<div id="vk_like"></div>
	</div>
	
	<?php 
	if (isset($this->user) && $this->isAllowed()) { 
	?>
	<div class="span2">
		<a href="javascript:void(0);" id="vklogin" class="vklogin" title="Нажмите чтобы войти через vkontakte"><img src="/images/forms/vklogin.png" alt="Войти vkontakte" /></a>
	</div>
	<?php 
	} ?>
    
</div>

<div class="container">
    <div class="yashare-auto-init" data-yashareL10n="ru" data-yashareType="button" data-yashareQuickServices="vkontakte,facebook,twitter,odnoklassniki,moimir,moikrug,gplus,surfingbird"></div>
</div>

